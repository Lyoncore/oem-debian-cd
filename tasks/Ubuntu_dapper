/*
 * This file is used to build a complete set of Dapper Drake CDs
 */

/* Packages that should really be on CD1 */
#if (CDIMAGE_INSTALL_BASE == 1)
#include <ubuntu/dapper/installer>
#endif

#if (CDIMAGE_INSTALL_BASE == 1)
#include <ubuntu/dapper/boot>
#include <ubuntu/dapper/minimal>
#include <ubuntu/dapper/standard>
#if (CDIMAGE_INSTALL == 1)
#include <ubuntu/dapper/desktop>
#include <ubuntu/dapper/ship>
#if (CDIMAGE_DVD == 1)
#include <ubuntu/dapper/supported>
#endif
#endif
#endif

#if (CDIMAGE_LIVE == 1)
#include <ubuntu/dapper/ship-live>
#endif
